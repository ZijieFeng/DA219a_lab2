import { makeStyles, Theme } from '@material-ui/core/styles'

export default makeStyles((theme: Theme) => ({
  progress: {
    margin: 'auto',
  },
  progressWrap: {
    display: 'flex',
    height: '100%',
    overflow: 'hidden',
  },
}))
